<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ evaluation.name }} — Evaluation Report</title>
  <style>
    body { font-family: 'Georgia', serif; max-width: 800px; margin: 2rem auto; padding: 0 1rem; color: #2d2014; background: #faf8f4; }
    h1 { font-size: 1.8rem; font-weight: 300; border-bottom: 1px solid #d4c5a9; padding-bottom: 0.5rem; }
    h2 { font-size: 1.3rem; font-weight: 400; margin-top: 2rem; color: #7a4c1e; }
    h3 { font-size: 1.1rem; font-weight: 500; }
    table { width: 100%; border-collapse: collapse; margin: 1rem 0; }
    th, td { padding: 0.5rem 0.75rem; text-align: left; border-bottom: 1px solid #e8dfd0; }
    th { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.05em; color: #8a7a5c; }
    .meta { color: #8a7a5c; font-size: 0.9rem; margin-bottom: 1.5rem; }
    .badge { display: inline-block; padding: 0.15rem 0.5rem; border-radius: 0.25rem; font-size: 0.8rem; background: #f0e8d8; }
    .rank-1 { background: #fef3c7; color: #92400e; }
    .rank-2 { background: #f3f4f6; color: #374151; }
    .rank-3 { background: #fed7aa; color: #9a3412; }
    footer { margin-top: 3rem; padding-top: 1rem; border-top: 1px solid #d4c5a9; font-size: 0.8rem; color: #8a7a5c; }
  </style>
</head>
<body>
  <h1>{{ evaluation.name }}</h1>
  <div class="meta">
    <span class="badge">{{ evaluation.perspective }}</span>
    <span class="badge">{{ evaluation.review_mode }} review</span>
    &middot; {{ total_responses }} responses &middot; {{ total_scores }} scores &middot; {{ reviewer_count }} reviewers
  </div>

  <h2>Model Rankings</h2>
  <table>
    <thead>
      <tr><th>Rank</th><th>Model</th><th>Overall Score</th></tr>
    </thead>
    <tbody>
      {% for entry in rankings %}
      <tr>
        <td><span class="badge rank-{{ entry.rank }}">{{ entry.rank }}</span></td>
        <td>{{ entry.model }}</td>
        <td>{{ entry.overall_score }} / 5.0</td>
      </tr>
      {% endfor %}
    </tbody>
  </table>

  <h2>Per-Model Dimension Scores</h2>
  {% for model, dims in model_averages.items() %}
  <h3>{{ model }} — {{ model_overall[model] }} overall</h3>
  <table>
    <thead>
      <tr><th>Dimension</th><th>Average</th></tr>
    </thead>
    <tbody>
      {% for dim, score in dims.items() %}
      <tr><td>{{ dim }}</td><td>{{ score }}</td></tr>
      {% endfor %}
    </tbody>
  </table>
  {% endfor %}

  <h2>Head-to-Head</h2>
  {% for model_a, comparisons in head_to_head.items() %}
  {% for model_b, dims in comparisons.items() %}
  <h3>{{ model_a }} vs {{ model_b }}</h3>
  <table>
    <thead>
      <tr><th>Dimension</th><th>Difference</th></tr>
    </thead>
    <tbody>
      {% for dim, diff in dims.items() %}
      <tr><td>{{ dim }}</td><td>{{ "+" if diff > 0 }}{{ diff }}</td></tr>
      {% endfor %}
    </tbody>
  </table>
  {% endfor %}
  {% endfor %}

  <footer>Report generated by Biblical Evals Framework</footer>
</body>
</html>
