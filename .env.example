# Root environment configuration for biblical-evals.
#
# Copy to .env and fill in values. The setup script (scripts/setup-env.sh)
# reads this file first, falling back to GCP Secret Manager for any
# values left empty.
#
# Usage:
#   cp .env.example .env
#   # Fill in values below
#   ./scripts/setup-env.sh          # Generates apps/api/.env.local and apps/web/.env.local

# --- GCP Project (temporary: trainerlab-prod — see #2) ---
GCP_PROJECT_ID=trainerlab-prod
GCP_REGION=us-west1

# --- Auth ---
NEXTAUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# --- LLM API Keys ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=

# --- Production URLs (set after first deploy) ---
# CLOUD_RUN_API_URL=
# VERCEL_WEB_URL=https://biblical-evals.vercel.app
